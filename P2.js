// For each loop in Array:----
//For each loop(direct iteration(line by line)of elements without
//need of array index)
var emp=[1,2,3,4,5,6,7]
var i, n=0
console.log("Elements Are:-----")
for(var chd of emp) // For each loop(direct iteration(line by line)of elements without
//need of array index)
{
    console.log(chd)
    n++
}
console.log("Total Elements are",n)